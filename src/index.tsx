import Providers from './providers';
import Navigator from './navigator';

const App = () => {
    return (
        <Providers>
            <Navigator />
        </Providers>
    );
};

export default App;
